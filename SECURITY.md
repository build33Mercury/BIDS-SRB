# Security policy

Do not report participant data, credentials, access tokens, private URLs, or protected health information in public issues. Security-sensitive reports should use the repository host's private security-reporting mechanism once the public repository is created. BIDS-SRB must never package FreeSurfer licenses or other private credentials.
