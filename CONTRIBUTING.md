# Contributing

Changes must preserve the precompute firewall, deterministic structured outputs, baseline immutability, and provenance. Do not add admitted scientific outcomes before COMPUTE-GO. New operator expectations require authority updates rather than adapter-local changes. Run `python -m unittest discover -s tests -v` before proposing changes.
